import { QueryController } from '../controllers/query-controller';
import { default as WyConversation } from './wy-conversation';
import { WeavyContextProps } from '../types/weavy.types';
import { MembersResultType } from '../types/members.types';
import { ConversationType } from '../types/conversations.types';
import { PropertyValues } from 'lit';

export default class WyConversationExtended extends WyConversation {
    protected conversationTitle: string;
    membersQuery: QueryController<MembersResultType>;
    isPrivateChat(conversation?: ConversationType): boolean;
    isChatRoom(conversation?: ConversationType): boolean;
    private handleRealtimeAppUpdated;
    private handleRealtimeSeenBy;
    private handleRealtimeDelivered;
    protected unsubscribeToRealtime(conversationId: number): void;
    protected willUpdate(changedProperties: PropertyValues<this & WeavyContextProps>): void;
    render(): import('lit-html').TemplateResult<1>;
}
//# sourceMappingURL=wy-conversation-extended.d.ts.map