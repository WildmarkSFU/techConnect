import { MembersResultType } from '../types/members.types';
import { AppType } from '../types/app.types';
import { FeaturesConfigType, FeaturesListType } from '../types/features.types';
import { UserType } from '../types/users.types';
import { InfiniteData } from '@tanstack/query-core';
import { MessagesResultType } from '../types/messages.types';
import { Ref } from 'lit/directives/ref.js';
import { LitElement } from 'lit';

export default class WyMessages extends LitElement {
    static styles: import('lit').CSSResult[];
    private weavyContext?;
    app: AppType;
    infiniteMessages: InfiniteData<MessagesResultType, unknown>;
    dataUpdatedAt: number;
    user: UserType;
    members?: MembersResultType;
    availableFeatures?: FeaturesListType;
    features?: FeaturesConfigType;
    unreadMarkerId?: number;
    unreadMarkerPosition?: "above" | "below";
    unreadMarkerShow: boolean;
    pagerRef: Ref<Element>;
    protected createRenderRoot(): HTMLElement | DocumentFragment;
    private dispatchVote;
    render(): import('lit-html').TemplateResult<1>;
}
//# sourceMappingURL=wy-messages.d.ts.map